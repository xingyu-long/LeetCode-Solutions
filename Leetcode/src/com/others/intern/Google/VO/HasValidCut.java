package com.intern.Google.VO;

public class HasValidCut {
    /*
    * 写一个function boolean hasValidCut(String a, String b)
return 能否找到一个cut把两个strings切成4份，crossover后产生一个palindrome。
* 注意crossover是指a的前一半+b的后一半，或者反过来，a的后一半+b的前一半。
    *
    * */
    // 这个有点像那个palindrome substring那个题。
}
